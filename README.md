(Unity) An rawimage-like compond with custom mesh. The click notification is shape-awared. And the shape can be animated by animation with the helper componet.
Currently the basic (polygon) type is ready.
